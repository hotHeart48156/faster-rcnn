project(network)
cmake_minimum_required(VERSION 3.16)
set(DEPENDS   Torch OpenCV TorchVision )
set(CMAKE_MODULE_PATH ${CMAKE_SOURCE_DIR}/builder)
message("CMAKE_SOURCE_DIR${CMAKE_SOURCE_DIR}")
include(library)
