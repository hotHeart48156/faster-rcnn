#pragma once
#include "voc_xml_data_model.h"
#include<string>
#include<rapidxml/rapidxml.hpp>
// namespace
// {

    class XmlParseRapid
    {
    private:
    Annotation annotation;

    public:
        XmlParseRapid(/* args */);
        ~XmlParseRapid();
        Annotation parse(std::string path);
        void next();
        rapidxml::xml_node<char> * parse_objects(rapidxml::xml_node<char> *node);
        rapidxml::xml_node<char> * parse_size(rapidxml::xml_node<char> *node);
    };

// }